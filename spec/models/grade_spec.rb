require 'spec_helper'

describe Grade do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    #Grade.create!(@valid_attributes)
  end
end
